import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CS430_FowlerW1Assignment',
  webDir: 'www'
};

export default config;
